'use strict';

/**
 * grid-dynamic-zone controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::grid-dynamic-zone.grid-dynamic-zone');
