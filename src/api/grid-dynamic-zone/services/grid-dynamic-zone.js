'use strict';

/**
 * grid-dynamic-zone service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::grid-dynamic-zone.grid-dynamic-zone');
